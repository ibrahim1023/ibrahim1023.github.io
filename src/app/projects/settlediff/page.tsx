import { PortfolioOverview } from "@/features/overview/PortfolioOverview";
export const metadata = { title: "SettleDiff — Ibrahim Arshad", alternates: { canonical: "/projects/settlediff/" } };
export default function Page() { return <PortfolioOverview project="settlediff" />; }
