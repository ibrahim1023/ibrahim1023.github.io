import { PortfolioOverview } from "@/features/overview/PortfolioOverview";
export const metadata = { title: "Vault Steward — Ibrahim Arshad", alternates: { canonical: "/projects/vault-steward/" } };
export default function Page() { return <PortfolioOverview project="vault-steward" />; }
